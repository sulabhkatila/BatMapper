name: iOS Build Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
- name: Build
  run: |
    xcodebuild \
      -scheme MBatmaN \
      -destination 'generic/platform=iOS Simulator' \
      CODE_SIGNING_ALLOWED=NO \
      build
